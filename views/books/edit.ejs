<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Book</title>
  <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
  <h1>Edit Book</h1>
  <form action="/books/<%= book._id %>?_method=PUT" method="POST">
    <div>
      <label for="title">Title</label>
      <input type="text" name="title" id="title" value="<%= book.title %>" required>
    </div>
    <div>
      <label for="author">Author</label>
      <input type="text" name="author" id="author" value="<%= book.author %>" required>
    </div>
    <div>
      <label for="genre">Genre</label>
      <input type="text" name="genre" id="genre" value="<%= book.genre %>" required>
    </div>
    <div>
      <label for="description">Description</label>
      <textarea name="description" id="description" required><%= book.description %></textarea>
    </div>
    <div>
      <label for="coverImageUrl">Cover Image URL</label>
      <input type="text" name="coverImageUrl" id="coverImageUrl" value="<%= book.coverImageUrl %>" required>
    </div>
    <button type="submit">Update Book</button>
  </form>
</body>
</html>
